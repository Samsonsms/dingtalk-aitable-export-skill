# 项目执行规范

- 默认中文；代码、命令和 Git 提交说明使用英文。
- 修改前读取本文件，检查 `git status --short`，保留用户改动。
- 本项目只负责通过官方 DWS CLI 原样导出用户选中的一张钉钉 AI 表格。
- `skills/` 是可分发技能；`tests/` 是隔离测试；`tools/` 是发布维护工具；`docs/` 是用户与维护文档。
- 临时文件放 `tmp/`，测试导出放 `output/`，发布包放 `dist/`；这些目录不入库。
- 不提交 `.env`、登录态、凭据、真实账号/base/table ID、导出文件或含业务数据的测试证据。
- 不调用 REST API，不通过记录查询重建 Excel，不下载整库，不修改原表、原视图或工作簿。
- 列表默认排除名称含“测试”的表。先列清单等用户选择，后导出；不按第一张表默认选择。
- 所有远端调用使用同一个固定 profile；权限失败停止，禁止切换账号绕过。
- 创建视图、提交导出结果不明时不得自动重放；只凭记录到状态文件的 viewId 清理。
- 变更脚本后运行 `python -m unittest discover -s tests -v`。发布另须通过技能结构校验和真实 CLI 冒烟测试。
- 发布工具必须绑定源码摘要和测试证据；源码变化后旧测试证据不可复用。
- 不覆盖已有发布包、安装目录或导出文件。升级先验证，再按文档备份旧版。
- 仅在测试通过后安装/打包；提交信息用英文。不自动 push，不执行破坏性 Git 重置。
